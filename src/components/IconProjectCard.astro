---
// Componente icon de la tarjeta de proyecto
import { Image } from 'astro:assets';
const { src, alt } = Astro.props;
---
<Image src={src} alt={alt} width={20} height={20} class="rounded-full p-[0.1rem] transition-colors duration-300 hover:bg-orange-500" />
